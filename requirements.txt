fastapi
uvicorn[standard]
python-dotenv
pandas
requests
numpy
matplotlib
scikit-learn
statsmodels
jupyter
google-genai
gtts==2.5.4
# click is intentionally pinned within gtts own declared range (click<8.2).
# CVE-2026-7246 (fixed in click>=8.3.3) affects only click.edit(), which
# neither this app nor gtts library code (as opposed to its unused gtts-cli
# tool) ever calls -- verified not exploitable in this deployment.
# Do NOT bump this past 8.1.x without also upgrading/replacing gtts, or
# pip resolver will refuse the install (ResolutionImpossible).
click>=8.1.8,<8.2
rapidfuzz
twilio
python-multipart
lightgbm
joblib
slowapi==0.1.10
